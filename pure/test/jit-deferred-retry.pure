// Failed deferred materialization attempts must leave a retained closure
// retryable. The focused CTest injects one failure before ORC submission and
// one after submission at lookup; the third call must materialize the same
// generation after both failures.

deferred_retry x = 42;
let retained = deferred_retry;

retained 2;
retained 2;
retained 2;

clear retained
clear deferred_retry
