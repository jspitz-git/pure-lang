using lv2, system;

lv2::controlin == (1,1) || exit 10;
lv2::audioin == (2,1) || exit 11;
lv2::audioout == (2,2) || exit 12;
lv2::midiin == (4,5) || exit 13;
lv2::time_midiin == (4,13) || exit 14;

puts "PURE_LV2_MODULE_OK";
