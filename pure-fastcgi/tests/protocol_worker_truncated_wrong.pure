using fastcgi;
using namespace fastcgi;
using system;

main =
  sleep 1 $$
  fprintf stdout "truncated-sentinel\n" () $$
  set_exit_status 72 $$
  finish $$
  finish
if accept >= 0 && getenv "TRUNCATED_SENTINEL" == "1";
= exit 70 otherwise;

main;
